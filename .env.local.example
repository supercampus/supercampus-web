# Keep browser requests same-origin and let Next.js proxy them to the separately deployed API.
NEXT_PUBLIC_API_URL=/api

# API base URL used by the Next.js server rewrite.
API_PROXY_TARGET=http://127.0.0.1:4000/api