# Frontend API URL — Must be /api so fetch requests match CSP connect-src 'self'
NEXT_PUBLIC_API_URL=/api

# Internal Express backend proxy target (Express runs on port 4000 internally)
API_PROXY_TARGET=http://127.0.0.1:4000/api
