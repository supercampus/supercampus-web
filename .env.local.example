# Keep frontend requests same-origin and let Next.js proxy them to Express.
NEXT_PUBLIC_API_URL=/api

# Local Express API. Override this in an ignored .env.local when needed.
API_PROXY_TARGET=http://127.0.0.1:4000/api
