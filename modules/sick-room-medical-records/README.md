# Sick Room & Medical Records frontend module

This package owns Sick Room & Medical Records navigation, permissions, runtime registration, screens, workflow documentation, and configuration adapters. Shared platform code belongs in packages.

