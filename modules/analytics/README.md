# Analytics & BI frontend module

This package owns Analytics & BI navigation, permissions, runtime registration, screens, workflow documentation, and configuration adapters. Shared platform code belongs in packages.

