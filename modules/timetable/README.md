# Timetable frontend module

This package owns Timetable navigation, permissions, runtime registration, screens, workflow documentation, and configuration adapters. Shared platform code belongs in packages.

