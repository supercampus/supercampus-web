# Visitor Management frontend module

This package owns Visitor Management navigation, permissions, runtime registration, screens, workflow documentation, and configuration adapters. Shared platform code belongs in packages.

